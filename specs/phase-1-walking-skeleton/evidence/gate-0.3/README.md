# Gate 0.3 evidence

This directory is reserved for redacted experiment output. The runner writes
`result.json` and `commands.json` only after the local containers are started.
No credentials are stored here. Gate 0 fast CI must not select the
`gate_0_3` marker; these experiments are explicit integration evidence.
